

<%= form_for(@my_erb_form, :template => 'custom', :html => { :class => 'string_template' }) do |f| %>
  <fieldset>
    
    <%= f.text_field :text_field, :template => {:hint => 'Hint me text_field.'} %>
    
  </fieldset>
<% end %>


