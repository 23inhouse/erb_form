<%= erb_form_for(@blog) do |f| %>
  <fieldset>
    <%= f.field :name, {
      :layout => 'simple_form',
      :error => 'My custom error',
      :hint => 'My custom hint',
      :input => {:size => 5},
      :label => 'My custom label' }
    %>
    <%= f.field :post, {
      :layout => 'simple_form',
      :label => 'My custom label' }
    %>
  </fieldset>
<% end %>
